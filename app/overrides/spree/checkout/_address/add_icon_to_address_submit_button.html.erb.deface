<!-- replace 'code:contains("submit_tag t(:save_and_continue)")' -->
<%= button_tag content_tag(:i, '', :class => 'icon-angle-right ') + ' ' + t(:save_and_continue), :type => 'submit', :class => 'continue button primary' %>